apiVersion: v1
kind: Service
metadata:
  name: flask-svc
  labels:
    app: flask-helloworld
spec:
  selector:
    app: flask-helloworld
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000

